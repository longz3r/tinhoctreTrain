// https://tinhoctre.vn/problem/thtbg2023bc_llqqdd

#include<iostream>
#include<string>

using namespace std;
int main() {
    int n, k;
    cin >> n >> k;
    cin.ignore();
    string input;
    getline(cin, input);
    cout << input;

    for (int start = 0; start < input.size() - 1; start++) {
        for (int substrlength = 1; substrlength < input.size() - start - 1) {
            
        }
    }
}